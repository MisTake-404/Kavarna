import Accordion from 'accordion-js';

/* DOCS: https://www.npmjs.com/package/accordion-js */

const accordion = (container, config) => {
  new Accordion(container, config);
}

export default accordion;

